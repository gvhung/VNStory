@model  VNStory.Web.Models.Category
@{
    ViewBag.Title = "thể loại";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">

    <h3>Bạn muốn xóa thể loại này phải không?</h3>
    <hr />

    <div class="mb-3">
        <label class="form-label">@Html.DisplayNameFor(model => model.Name)</label>
    </div>

    <div class="mb-3">
        <label class="form-label">@Html.DisplayFor(model => model.Name)</label>
    </div>

    <div class="form-actions no-color">

        @using (Html.BeginForm("Delete", "Category", FormMethod.Post, new { role = "form" }))
        {
            @Html.AntiForgeryToken()
            <input type="submit" value="Xóa thể loại này" class="btn btn-danger" />

            @Html.ActionLink("Back to List", "Index", "Category", routeValues: new { Area = "Admin" }, htmlAttributes: new { @class = "btn btn-secondary" });
        }

    </div>

</div>